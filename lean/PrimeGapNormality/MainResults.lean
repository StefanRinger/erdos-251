import PrimeGapNormality.CorePaperMain
import PrimeGapNormality.Prime.CoreRoughLogSlopeClassification
import PrimeGapNormality.Prime.CoreRoughJointEnd
import PrimeGapNormality.Prime.CoreRoughDimensionEnd
import PrimeGapNormality.Prime.CoreRoundedHeadline
import PrimeGapNormality.Prime.CorePrimeQuantitativeEnd

/-!
# Main results

This import-only module collects:

* the conditional qualitative prime package (D, small AHL, and Kuperberg
  routes), including local classification, relations, joint laws, dimensions,
  and periodic position weights;
* the unconditional moving-rough local classification and its explicit
  logarithmic-slope position, joint, and dimension endpoints;
* the literal rounded-power prime headline; and
* the quantitative prime-position discrepancy and uniform digit-word bounds.

For the complete results, including general cutoff mixtures, auxiliary
estimates and subsequent corollaries, import `PrimeGapNormality.Paper`.
Compile `PrimeGapNormality.PaperAudit` to inspect the full endpoint surface.
-/
