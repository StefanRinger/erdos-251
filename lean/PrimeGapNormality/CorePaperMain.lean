import PrimeGapNormality.CoreAHLPrime
import PrimeGapNormality.Prime.CorePrimeLocalDimensionEnd
import PrimeGapNormality.Prime.CorePositionComponents
import PrimeGapNormality.Prime.CoreCommonPeriodRelations

/-! Curated conditional polynomial main theorem and its finite-family
corollaries, including the actual dimension of the series span.

`CoreLocalPrime` keeps the D/Kuperberg path separate from the classical
normalization used by the small-AHL adapter. This combined entry includes
both paths. It does not claim the still separate unconditional rough,
rounded-real-power or quantitative-discrepancy conclusions. -/
