import PrimeGapNormality.CoreLocalPrime
import PrimeGapNormality.Prime.CorePrimeLocalAHL

/-! Curated small-window one-sided AHL endpoint. The explicit classical
PNT normalization is proved in the imported pinned library slice; it is
not an extra end-theorem assumption. The D/Kuperberg-only entry remains
`CoreLocalPrime`, independently usable without this import. -/
